
[ -n "$BASH_COMPLETION" ] || export BASH_COMPLETION=~/bash_completion
[ -n "$BASH_COMPLETION_DIR" ] || export BASH_COMPLETION_DIR=~/bash_completion.d
[ -n "$BASH_COMPLETION_COMPAT_DIR" ] || export BASH_COMPLETION_COMPAT_DIR=~/bash_completion.d

. ~/bash_completion
